# Python
__pycache__/
*.py[cod]
*$py.class
*.so

# Environments
venv/
env/
ENV/
env.bak/
venv.bak/
.venv/
pptx_env/
.env
messages.mo
sent_emails/

# Django
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
db.sqlite3.backup-*
staticfiles/

# The compiled catalogue (locale/pl/LC_MESSAGES/django.mo) is committed on
# purpose: the Polish interface must work even if the deploy image has no
# gettext binaries to run `compilemessages`.

# MacOS
.DS_Store

# IDEs
.vscode/
.idea/
.codegraph/

# Scratch left behind by the screenshot capture used for the defence deck
docs/.chrome-profile/
node_modules/

# CI workflow kept local until a workflow-scoped token is set
.github/workflows/ci.yml

# Another student's deck and scratch files do not belong in this repository.
BloomTime_Diploma_Presentation_*.pptx
parse_pptx.py
